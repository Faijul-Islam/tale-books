import 'package:flutter/material.dart';

class TotalCalculation extends StatefulWidget {
  const TotalCalculation({Key? key}) : super(key: key);

  @override
  State<TotalCalculation> createState() => _TotalCalculationState();
}

class _TotalCalculationState extends State<TotalCalculation> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Text("data")
        ],
      ),
    );
  }
}
